# DragonsDash Environment Variables
# NEVER commit actual values — this file is for reference only

# Market data API keys (user-provided, optional)
IEX_CLOUD_API_KEY=
ALPHA_VANTAGE_API_KEY=
POLYGON_IO_API_KEY=

# CoinGecko (free tier — no key required for basic quotes)
COINGECKO_API_KEY=

# WebAuthn Relying Party
NEXT_PUBLIC_WEBAUTHN_RP_ID=localhost
NEXT_PUBLIC_WEBAUTHN_RP_NAME=DragonsDash

# Database (PostgreSQL for encrypted blob storage)
DATABASE_URL=
